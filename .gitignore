.DS_Store
# personal tandem defaults of anyone using the symlink install — never committed to this repo
skills/*/config.md
# tandem run state (working memory only — persistent defaults live with the skill install)
.tandem/
# skill-creator eval workspaces (see docs/VALIDATION.md)
*-workspace/
